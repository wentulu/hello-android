

rootProject.ext {
    compileSdkVersion = 30
    buildToolsVersion="30.0.3"
    minSdkVersion=16
    targetSdkVersion=30
    versionCode=1
    versionName="1.0"
}

task hello {
    print("hello $project.name")
}